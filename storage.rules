
rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    function isAdmin() {
      return request.auth != null && request.auth.token.email == 'admin@riverph.com';
    }

    // A user can only write to their own folder.
    match /users/{userId}/{allPaths=**} {
      allow read: if request.auth != null; // Allow any auth'd user to read (e.g. for profile pics)
      allow write: if (request.auth != null && request.auth.uid == userId) || isAdmin();
    }

    // Admins can manage station documents. Any auth'd user can read them.
    match /stations/{stationId}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if isAdmin();
    }
    
    // Only admins can upload contracts. The specific user can also read their own contract.
    match /userContracts/{userId}/{allPaths=**} {
      allow read: if (request.auth != null && request.auth.uid == userId) || isAdmin();
      allow write: if isAdmin();
    }
  }
}
