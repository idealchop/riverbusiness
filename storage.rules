rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // WARNING: Broad rule for debugging purposes.
    // Allows any authenticated user to write anywhere.
    match /{allPaths=**} {
      allow write: if request.auth != null;
    }
  }
}
