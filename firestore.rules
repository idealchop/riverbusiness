
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    function isOwner(userId) {
      return request.auth != null && request.auth.uid == userId;
    }

    function isAdmin() {
      return request.auth != null && request.auth.token.email == 'admin@riverph.com';
    }

    match /users/{userId} {
      allow read: if isOwner(userId) || isAdmin();
      allow create: if request.auth != null;
      allow update: if isOwner(userId) || isAdmin();
      allow delete: if isOwner(userId);
    }
    
    match /users/{userId}/deliveries/{deliveryId} {
      allow read, list: if isOwner(userId) || isAdmin();
      allow create, update, delete: if isAdmin();
    }

    match /users/{userId}/payments/{paymentId} {
      allow read, list: if isOwner(userId) || isAdmin();
      // Allow admins to update payment status or create an invoice
      allow update, create: if isAdmin();
    }

    // This rule allows admins to query the 'payments' collection group
    match /{path=**}/payments/{paymentId} {
      allow read, list: if isAdmin();
    }
    
    match /users/{userId}/consumptionHistory/{historyId} {
      allow read, list: if isOwner(userId) || isAdmin();
      allow create, update: if isAdmin();
    }

    match /users/{userId}/notifications/{notificationId} {
      allow read, list: if isOwner(userId);
      allow update: if isOwner(userId); // Allow user to mark as read
      allow create: if isOwner(userId) || isAdmin(); 
      allow delete: if isAdmin(); 
    }

    match /users/{userId}/sanitationVisits/{visitId} {
      allow read, list: if isOwner(userId) || isAdmin();
      allow create, update, delete: if isAdmin();
    }

    match /users/{userId}/refillRequests/{requestId} {
      // User can create, read, and list their own requests.
      allow create: if isOwner(userId);
      allow read, list: if isOwner(userId) || isAdmin();
      // Admins can update any request (e.g., to change status).
      allow update: if isAdmin();
    }

    // This rule allows admins to query the 'refillRequests' collection group
    match /{path=**}/refillRequests/{requestId} {
      allow read, list: if isAdmin();
    }

    match /waterStations/{stationId} {
      allow read: if request.auth != null;
      allow create, update, delete: if isAdmin();
    }
    
    match /waterStations/{stationId}/{document=**} {
        allow read, list: if request.auth != null;
        allow create, update, delete: if isAdmin();
    }

    match /schedules/currentSchedule {
      allow read: if request.auth != null;
      allow write: if isAdmin();
    }

  }
}
