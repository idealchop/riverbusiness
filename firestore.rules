
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allows everyone to read (for debugging ONLY)
    match /{document=**} {
      allow read: true;
      allow write: if request.auth != null;
    }
  }
}
